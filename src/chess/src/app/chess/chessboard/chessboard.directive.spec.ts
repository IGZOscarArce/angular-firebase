import { ChessboardDirective } from './chessboard.directive';

describe('ChessboardDirective', () => {
  it('should create an instance', () => {
    const directive = new ChessboardDirective();
    expect(directive).toBeTruthy();
  });
});
